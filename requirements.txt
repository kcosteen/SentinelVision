# Core computer-vision pipeline
opencv-python
mediapipe
numpy

# Real-time object detection (YOLOv8)
ultralytics

# Data wrangling for the Phase 1 training dataset (windowed feature aggregation)
pandas

# Phase 1 model training + evaluation
scikit-learn
joblib

# Phase 2: downloading public detection datasets from the Hugging Face Hub,
# and reading the data.yaml class lists that ship with Roboflow YOLO exports
huggingface_hub
pyyaml

# Demo web app (upload a clip, see the proctoring result)
streamlit
